[profile.default]
src = "src"
out = "out"
libs = ["lib"]

[profile.ci]
solc-version = "0.8.25"
via-ir = true

[rpc_endpoints]
localhost = "http://localhost:8545"
sepolia = "${SEPOLIA_RPC_URL}"
mainnet = "${MAINNET_RPC_URL}"
